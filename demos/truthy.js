
    function logTruthiness(val) {
        if (val) {
            console.log("Truthy!");
        } else {
            console.log("Falsy.");
        }
    }

    //logTruthiness(true);

    logTruthiness({});

    //logTruthiness([]);

    //logTruthiness("some string");

    //logTruthiness(3.14);

    //logTruthiness(new Date());
    
    var foo="test";
    if (foo){
      console.log(foo)
    }
    else{
        console.log("nope");
    }
        
